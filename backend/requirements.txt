# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30

# Settings + validation
pydantic>=2.7
pydantic-settings>=2.3

# HTTP client for the MediaWiki API
httpx>=0.27

# PostgreSQL
SQLAlchemy[asyncio]>=2.0.30
psycopg[binary]>=3.1

# Tests
pytest>=8.0
pytest-asyncio>=0.23
# Starlette's TestClient moved to httpx2; httpx stays the runtime client.
httpx2>=2.0
