# Base URL of the FastAPI backend.
# Leave unset to use the Vite dev-server proxy (/api -> http://127.0.0.1:8000).
# Set it to call a backend directly, e.g. http://127.0.0.1:8000/api
# VITE_API_BASE_URL=
